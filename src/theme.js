import { createTheme } from '@mui/material/styles';

export const appTheme = createTheme({
  palette: {
    primary: {
      main: '#65B8BF',
    },
    secondary: {
      main: '#8F8CF2',
    },
  },
});
